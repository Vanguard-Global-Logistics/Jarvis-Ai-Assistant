# The archived design handoff is immutable — never reformat it (CLAUDE.md §0).
reference/

# Authoritative prose documents. Prettier's markdown table alignment rewrites
# these wholesale, which buries real edits in reformatting noise and makes the
# git history of the project's contract harder to read. They are prose reviewed
# by a human, not code — formatting them buys nothing.
CLAUDE.md
docs/CURRENT-STATE-AUDIT.md
docs/VISUAL-DESIGN-TARGET.md

# Git-ignored subagent scratch space (briefs, reports, review packages) —
# working files, not source; they must never gate `npm run verify`.
.superpowers/

# Build output and dependencies
dist/
out/
node_modules/
coverage/
.vite/
package-lock.json
