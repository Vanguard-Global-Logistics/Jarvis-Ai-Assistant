{
  "name": "@jarvis/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas and inferred types shared across every workspace. The single definition of every cross-boundary contract.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./ipc/channels": "./src/ipc/channels.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
