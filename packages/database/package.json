{
  "name": "@jarvis/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SQLite foundation on Node's built-in node:sqlite: connection ownership, a forward-only migration runner, and the conversation-history schema.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
