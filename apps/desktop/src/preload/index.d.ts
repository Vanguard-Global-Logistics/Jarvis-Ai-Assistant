/**
 * The renderer's view of the preload bridge.
 *
 * Empty because the bridge is empty. When a channel is added to
 * ./index.ts, its type is declared here — sourced from @jarvis/contracts, never
 * hand-written a second time (CLAUDE.md §3: no duplicated logic).
 */

declare global {
  // No `window.jarvis` exists. Nothing is exposed.
}

export {};
